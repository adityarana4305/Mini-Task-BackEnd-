<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>project-1</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-950">
        <div class="form p-5 text-white ">
            <form action="/create" method="post" >
                <input type="text" placeholder="Title" class="block px-5 py-2 w-full bg-zinc-800 m-4 rounded-md" name="title">
                <textarea placeholder="Write your tasks" class="block px-5 py-2 resize-none w-full bg-zinc-800 m-4 rounded-md" name="description"></textarea>
                <input type="submit" id="btn" class="w-50 bg-blue-500 rounded-md ml-4 px-5 py-2" value="Add it">
                
            </form>
        </div>
        <div class="tasks p-10 text-white flex flex-wrap gap-5">
            <% if(files.length > 0){ %>
                <% files.forEach((val)=>{ %>
                    <div class="task py-5 px-3 bg-zinc-800 w-74 rounded-md " > 
                <h3 class="text-3xl font-bold tracking-loose"><%= val.replace(/.txt/g,""); %></h3> 
                <div class="flex mt-3 text-blue-500 justify-between">
               <a href="/files/<%= val %>" >Read More...</a>
               <a href="/edit/<%= val %>" >Edit🖊️</a>
               </div>
            </div> 
              <%  }) %>
               <% } else { %>
                <h3 class="text-3xl font-bold text-red-500">NO TASKS ARE PRESENT</h3>
              <% } %>   
        </div>
    </div>
    <script src="javascripts/script.js"></script>
</body>
</html>